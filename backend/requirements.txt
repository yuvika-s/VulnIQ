fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
networkx>=3.2
anthropic>=0.39
httpx>=0.27
truststore>=0.9   # trust the OS/corporate root store (Zscaler etc.) for TLS
python-multipart>=0.0.9
openpyxl>=3.1
pypdf>=4.2
# persistence (PostgreSQL) — optional; engine runs in JSON mode when DATABASE_URL is unset
SQLAlchemy>=2.0
alembic>=1.13
psycopg2-binary>=2.9   # PostgreSQL driver
boto3>=1.34            # AWS Secrets Manager (fetch RDS creds on EC2)
